import Link from "next/link";import {money} from "@/lib/api";
export type Car={id:number;name:string;licensePlate:string;dailyPrice:number;description:string;imageUrl?:string;location:string;modelYear:number;status:string;brand:{name:string};carType:{name:string;seats:number}};
export default function CarCard({car}:{car:Car}){return <article className="car-card"><div className="car-image" style={{backgroundImage:`linear-gradient(180deg,transparent 45%,rgba(4,18,27,.72)),url(${car.imageUrl||""})`}}><span className="pill">{car.status==="AVAILABLE"?"Sẵn sàng":"Tạm hết"}</span><span className="location">⌖ {car.location}</span></div><div className="car-body"><div className="muted">{car.brand.name} · {car.carType.name} · {car.carType.seats} chỗ</div><h3>{car.name}</h3><p>{car.description}</p><div className="car-footer"><div><strong>{money(car.dailyPrice)}</strong><small>/ ngày</small></div><Link href={`/cars/${car.id}`} className="arrow">Xem xe →</Link></div></div></article>}

