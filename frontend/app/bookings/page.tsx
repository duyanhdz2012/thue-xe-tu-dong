"use client";
import{useEffect,useState}from"react";import{api,money}from"@/lib/api";
type Booking={id:number;car:{name:string};pickupDate:string;returnDate:string;totalAmount:number;paidAmount:number;status:string};
export default function Bookings(){const[list,setList]=useState<Booking[]>([]);const[error,setError]=useState("");const load=()=>api<Booking[]>("/api/bookings/mine").then(setList).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function cancel(id:number){await api(`/api/bookings/${id}`,{method:"DELETE"});void load()}return <section className="section"><div className="eyebrow">LỊCH SỬ THUÊ XE</div><h1>Đơn của tôi</h1>{error&&<p className="error">{error}</p>}<div className="booking-list">{list.map(b=><article key={b.id}><div><span className="pill dark">{b.status}</span><h3>{b.car.name}</h3><p>{b.pickupDate} → {b.returnDate}</p></div><div><strong>{money(b.totalAmount)}</strong><p>Đã trả {money(b.paidAmount)}</p>{['PENDING','APPROVED'].includes(b.status)&&<button className="ghost"onClick={()=>cancel(b.id)}>Hủy đơn</button>}</div></article>)}</div></section>}
