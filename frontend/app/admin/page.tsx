"use client";
import{useEffect,useState}from"react";import{api,getSession,money}from"@/lib/api";
type Stats={users:number;cars:number;bookings:number;pendingBookings:number;revenue:number};type Booking={id:number;customer:{name:string};car:{name:string};pickupDate:string;returnDate:string;totalAmount:number;status:string};
export default function Admin(){const[stats,setStats]=useState<Stats|null>(null);const[bookings,setBookings]=useState<Booking[]>([]);const load=()=>{api<Stats>("/api/dashboard").then(setStats);api<Booking[]>("/api/bookings").then(setBookings)};useEffect(()=>{if(getSession()?.role!=="ADMIN")location.href="/login";else load()},[]);async function status(id:number,value:string){await api(`/api/bookings/${id}/status?value=${value}`,{method:"PATCH"});load()}return <section className="admin"><div className="admin-title"><div><div className="eyebrow">TRUNG TÂM ĐIỀU HÀNH</div><h1>Tổng quan kinh doanh</h1></div><a className="button small"href="http://localhost:8080/swagger-ui.html"target="_blank">Swagger API</a></div>{stats&&<div className="stat-grid"><div><span>Khách hàng</span><b>{stats.users}</b></div><div><span>Đội xe</span><b>{stats.cars}</b></div><div><span>Đơn đặt</span><b>{stats.bookings}</b></div><div><span>Doanh thu</span><b>{money(stats.revenue)}</b></div></div>}<h2>Đơn cần xử lý</h2><div className="table-wrap"><table><thead><tr><th>Mã</th><th>Khách hàng</th><th>Xe</th><th>Lịch thuê</th><th>Giá trị</th><th>Trạng thái</th></tr></thead><tbody>{bookings.map(b=><tr key={b.id}><td>#{b.id}</td><td>{b.customer.name}</td><td>{b.car.name}</td><td>{b.pickupDate}<br/>{b.returnDate}</td><td>{money(b.totalAmount)}</td><td><select value={b.status}onChange={e=>status(b.id,e.target.value)}><option>PENDING</option><option>APPROVED</option><option>ACTIVE</option><option>COMPLETED</option><option>CANCELLED</option></select></td></tr>)}</tbody></table></div></section>}
