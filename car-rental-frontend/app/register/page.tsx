"use client";
import{useState}from"react";import{api,setSession,Session}from"@/lib/api";
export default function Register(){const[form,setForm]=useState({name:"",email:"",phone:"",password:""});const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();try{const s=await api<Session>("/api/auth/register",{method:"POST",body:JSON.stringify(form)});setSession(s);location.href="/cars"}catch(x){setError((x as Error).message)}}return <section className="auth"><form onSubmit={submit}><div className="eyebrow">TẠO TÀI KHOẢN</div><h1>Bắt đầu hành trình</h1>{[['name','Họ và tên'],['email','Email'],['phone','Số điện thoại'],['password','Mật khẩu']].map(([k,l])=><label key={k}>{l}<input type={k==='password'?'password':k==='email'?'email':'text'}value={form[k as keyof typeof form]}onChange={e=>setForm({...form,[k]:e.target.value})}required={k!=='phone'}/></label>)}{error&&<p className="error">{error}</p>}<button className="button">Đăng ký</button></form></section>}

