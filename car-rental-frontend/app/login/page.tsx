"use client";
import Link from"next/link";import{useState}from"react";import{api,setSession,Session}from"@/lib/api";
export default function Login(){const[email,setEmail]=useState("customer@carrental.vn");const[password,setPassword]=useState("Customer@123");const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();try{const s=await api<Session>("/api/auth/login",{method:"POST",body:JSON.stringify({email,password})});setSession(s);location.href=s.role==="ADMIN"?"/admin":"/cars"}catch(x){setError((x as Error).message)}}return <section className="auth"><form onSubmit={submit}><div className="eyebrow">CHÀO MỪNG TRỞ LẠI</div><h1>Đăng nhập</h1><p className="muted">Quản lý hành trình và các đơn thuê xe của bạn.</p><label>Email<input type="email"value={email}onChange={e=>setEmail(e.target.value)}required/></label><label>Mật khẩu<input type="password"value={password}onChange={e=>setPassword(e.target.value)}required/></label>{error&&<p className="error">{error}</p>}<button className="button">Đăng nhập</button><p>Chưa có tài khoản? <Link href="/register">Đăng ký</Link></p></form></section>}

