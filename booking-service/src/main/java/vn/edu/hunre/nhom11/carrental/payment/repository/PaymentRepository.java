package vn.edu.hunre.nhom11.carrental.payment.repository;import java.math.BigDecimal;import java.util.*;import org.springframework.data.jpa.repository.*;import vn.edu.hunre.nhom11.carrental.payment.domain.Payment;public interface PaymentRepository extends JpaRepository<Payment,Long>{List<Payment>findByBookingId(Long bookingId);List<Payment>findByBookingIdAndStatus(Long bookingId,Payment.Status status);Optional<Payment>findByTransactionCodeIgnoreCase(String transactionCode);boolean existsByExternalTransactionId(String externalTransactionId);long countByStatus(Payment.Status status);@Query("select coalesce(sum(p.amount),0) from Payment p where p.status='SUCCESS'")BigDecimal totalRevenue();}
